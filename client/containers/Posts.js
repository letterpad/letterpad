import React from "react";

const Posts = () => {
    return (
        <div>
            This is just a abstract component. You should create a theme and
            keep this File inside containers directory
        </div>
    );
};

export default Posts;
