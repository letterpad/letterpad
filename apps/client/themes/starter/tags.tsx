import { InferGetServerSidePropsType } from 'next';

// import { getServerSideProps } from '../../src/app/tags/page';

export const Tags = ({ tags, me, settings }) => {
  return <div>Display a list of cliclable tags</div>;
};
